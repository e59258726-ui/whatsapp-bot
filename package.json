FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production && npm cache clean --force

COPY . .

RUN mkdir -p sessions

ENV PORT=10000

EXPOSE 10000

CMD ["node", "index.js"]
